# 🚀 ازاي ترفع موقع Friends Hub ويشتغل أونلاين

الموقع حالياً شغال على جهازك (Localhost). عشان أي حد من صحابك يقدر يفتحه من موبايله، لازم نرفعه على النت. هنستخدم خدمة اسمها **Vercel** (مجانية وسريعة جداً).

## الخطوة 1: ارفع الكود على GitHub
لازم الكود يكون موجود على موقع GitHub الأول.
1. ادخل على [GitHub.com](https://github.com) واعمل حساب لو معندكش.
2. اعمل **New Repository** وسميه مثلاً `friends-hub`.
3. خد الرابط بتاعه (اللي شبه `https://github.com/YourName/friends-hub.git`).
4. افتح "الترمينال" (Terminal) عندك في المشروع واكتب الأوامر دي بالترتيب:
   ```bash
   git init
   git add .
   git commit -m "موقع الصحاب وصل"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/friends-hub.git
   # بدل الرابط ده بالرابط بتاعك اللي خدته من الموقع
   git push -u origin main
   ```

## الخطوة 2: شغل الموقع على Vercel
1. ادخل على [Vercel.com](https://vercel.com) وسجل دخول بـ GitHub بتاعك.
2. دوس **Add New** > **Project**.
3. هتلاقي `friends-hub` ظهر قدامك، دوس **Import**.
4. **مهم جداً**: هتلاقي خانة اسمها **Environment Variables** (المتغيرات البيئية). لازم تضيف فيها البيانات دي عشان قاعدة البيانات تشتغل:
   - في الخانة `Key` اكتب: `VITE_SUPABASE_URL`
   - في الخانة `Value` حط رابط Supabase بتاعك.
   - دوس Add.
   - كررها تاني:
   - في الخانة `Key` اكتب: `VITE_SUPABASE_ANON_KEY`
   - في الخانة `Value` حط الـ Key بتاعك من Supabase.
5. دوس **Deploy**.

## الخطوة 3: تفعيل الصور (Supabase Storage)
عشان الصور تشتغل، لازم تعمل مخزن (Bucket) في Supabase:
1. ارجع لـ Supabase Dashboard.
2. اختار **Storage** من القائمة اللي في الجنب.
3. دوس **New Bucket**.
4. سميه: `gallery` (لازم الاسم ده بالضبط).
5. **شغل الزرار**: "Public Bucket" (عشان الصور تظهر للناس).
6. دوس Save.

## مبروك! 🎉
Vercel هيديك رابط للموقع (مثلاً `friends-hub.vercel.app`). انسخه وابعه لصحابك!
- أي حد هيكتب اسمه هيدخل فوراً.
- كله هيشوف بعضه لايف (خريطة، شات، صور).

⚠️ **ملحوظة أمان**: النظام ده "بسيط" (Simple Auth). يعني أي حد كاتب اسم "أحمد" هيدخل كأنه أحمد. بما إنه لصحابك بس، الموضوع قشطة، بس بلاش تنشر الرابط في مكان عام أوي.
